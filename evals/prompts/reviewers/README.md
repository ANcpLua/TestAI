# Reviewer prompts

Runtime reviewer prompts live in `src/AiOnlyEval.Core/Reviewers/ReviewerPromptLibrary.cs`.

Project-scoped Codex reviewer agents live in `.codex/agents/*.toml`.

The same reviewer names are enforced by `evals/thresholds/default-gates.json`.
